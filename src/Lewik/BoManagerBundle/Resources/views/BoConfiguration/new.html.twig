{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BoConfiguration creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('boconfiguration') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
