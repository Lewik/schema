{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BoFieldConfiguration list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Label</th>
                <th>Systemname</th>
                <th>Type</th>
                <th>Length</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('bofieldconfiguration_show', { 'id': entity.id }) }}">{{ entity.label }}</a></td>
                <td>{{ entity.systemName }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.length }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('bofieldconfiguration_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('bofieldconfiguration_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('bofieldconfiguration_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
