{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BoFieldConfiguration</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Label</th>
                <td>{{ entity.label }}</td>
            </tr>
            <tr>
                <th>Systemname</th>
                <td>{{ entity.systemName }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity.type }}</td>
            </tr>
            <tr>
                <th>Length</th>
                <td>{{ entity.length }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('bofieldconfiguration') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('bofieldconfiguration_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
