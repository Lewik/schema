{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BoFieldConfiguration edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('boconfiguration_edit', { 'id': entity.bo.id }) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
